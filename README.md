# SmartProxy — Умный прокси для обхода блокировок

Программа для Windows, которая автоматически направляет трафик заблокированных сайтов через прокси-сервер в Латвии, а остальной трафик идёт напрямую.

## Архитектура

```
┌─────────────────┐     ┌──────────────────┐     ┌─────────────────┐
│   Ваш браузер   │────▶│   SmartProxy     │────▶│  Сервер Латвия  │──▶ Заблокированные
│                 │     │   (локально)     │     │  (SOCKS5)       │    сайты
└─────────────────┘     │                  │     └─────────────────┘
                        │  Проверяет домен │
                        │                  │────▶ Напрямую ──────────▶ Обычные сайты
                        └──────────────────┘
```

## Компоненты

### 1. Сервер (Ubuntu 22.04 в Латвии)
- **Софт:** 3proxy (SOCKS5 прокси)
- **Порт:** 18388

### 2. Клиент (Windows)
- **Софт:** SmartProxy.exe
- **Локальный порт:** 8080 (HTTP прокси для браузера)

## Список проксируемых доменов

- `gemini.google.com` и поддомены
- `notebooklm.google.com` и поддомены  
- `openai.com`, `chat.openai.com` и поддомены
- `youtube.com`, `youtu.be`, `googlevideo.com`
- `instagram.com`, `cdninstagram.com`
- `higgsfield.ai`

## Установка

### Сервер
```bash
# Подключиться по SSH и запустить скрипт установки
ssh root@193.242.109.75
# Выполнить команды из server/install.sh
```

### Клиент (Windows)
1. Скачать `SmartProxy.exe`
2. Запустить программу
3. Настроить браузер на использование прокси `127.0.0.1:8080`

## Файлы проекта

```
/
├── README.md              # Этот файл
├── server/
│   ├── install.sh         # Скрипт установки сервера
│   └── 3proxy.cfg         # Конфигурация прокси
├── client/
│   ├── smartproxy.py      # Основной код клиента
│   ├── config.yaml        # Настройки и список доменов
│   └── build.bat          # Сборка в .exe
└── docs/
    └── SERVER_INFO.md     # Данные сервера (приватно)
```
